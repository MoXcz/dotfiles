import QtQuick
import qs.Commons

// Muted uppercase section label used inside bar panels.
Text {
  color: Theme.muted
  font.family: Theme.fontFamily
  font.pixelSize: Theme.fontCaption
  font.capitalization: Font.AllUppercase
  font.letterSpacing: 1
}
